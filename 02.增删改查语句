CREATE TABLE `goods` (
	id INT ,
	goods_name VARCHAR(10), -- 长度10
	price DOUBLE NOT NULL DEFAULT 100 );

1.insert语句
INSERT INTO `goods` (id, goods_name, price) 
	VALUES(10, '华为手机', 2000);
INSERT INTO `goods` (id, goods_name, price) 
	VALUES(20, '苹果手机', 3000);
2.update语句
update `goods` set price=price+1000 where id=10;
3.delete 语句 delete是删除表记录，drop是删除表
delete from `goods` where id=10;
4.select语句：重点查询语句
基本语法
select 列名 from 表明 各种子句
常用
1.select*from `goods`；
2.select disctint 列名 from 表名 where 筛选子句 

truncate
rownum
